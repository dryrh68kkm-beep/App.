# มาตรการป้องกันข้อมูลรั่วไหล · DeptFlow (แอปมือถือ)

ข้อมูลที่ต้องปกป้อง ได้แก่ ชื่อ รหัสพนักงาน เวลาทำงาน การขาดงาน และการลา

## สิ่งที่อยู่บน GitHub

- **เฉพาะโค้ดของแอป** ตามที่เจ้าของโปรเจกต์อนุญาตให้ใช้ GitHub Pages
- ไม่มี PDF, ZIP, ภาพหน้าจอ, log หรือข้อมูลพนักงาน (มี `.gitignore` กันไว้อีกชั้น)
- ข้อมูลในเทสต์เป็นข้อมูลจำลองทั้งหมด เช่น รหัส `99xxxxx` และชื่อ "นายทดสอบ สมมติ"
- `config.js` มีเฉพาะชื่อแผนกของสาขา ไม่มีข้อมูลส่วนบุคคล
- repository เป็นสาธารณะ ใครก็อ่านโค้ดได้ แต่ไม่มีข้อมูลพนักงานให้อ่าน

## การทำงานในมือถือ

- เปิด อ่าน และแยก PDF **ในเบราว์เซอร์ของมือถือเครื่องนั้น** ด้วย pdf.js และ pdf-lib ที่ฝังอยู่ในแอป
- ไม่มีเซิร์ฟเวอร์ประมวลผล ไม่มี API ภายนอก ไม่ใช้ OCR ไม่มี analytics
- Content-Security-Policy อนุญาตให้โหลดและเชื่อมต่อได้เฉพาะไฟล์ของแอปเอง (`'self'`) และ `blob:` / `data:` ในเครื่อง เว็บไซต์อื่นจึงเรียกไม่ได้
- เอกสารอยู่ใน**หน่วยความจำ**เท่านั้น ไม่ถูกเขียนลง localStorage, IndexedDB หรือ cache และถูกลบเมื่อกดล้างข้อมูลหรือปิดแอป
- service worker เก็บ**เฉพาะไฟล์ของแอป** ไว้ใช้ออฟไลน์ ไม่เก็บเอกสาร
- localStorage เก็บเฉพาะชื่อแผนกที่ HR พิมพ์เพิ่ม เพื่อให้เลือกครั้งต่อไปได้ ไม่มีข้อมูลพนักงาน

## ความถูกต้องของเอกสาร

- แอป**คัดลอกหน้า**จากต้นฉบับเท่านั้น (`copyPages`) ไม่แก้เวลาทำงาน การขาดงาน การลา หรือเนื้อหาใด ๆ
- ตรวจว่าทุกหน้าอยู่ในกลุ่มเดียวเท่านั้น ถ้าไม่ครบหรือซ้ำ แอปจะหยุด
- หลังสร้างไฟล์ ตรวจจำนวนหน้าและขนาดหน้าของทุกไฟล์เทียบกับต้นฉบับ
- เทสต์ครบขั้นตอนยืนยันแล้วว่าข้อความและภาพสแกนของทุกหน้าในไฟล์ที่แยกออกมา เหมือนต้นฉบับทุกไบต์
- หน้าที่ไม่ระบุแผนกไปอยู่ไฟล์ **รอตรวจสอบ** และแอปถามยืนยันก่อนสร้าง ไม่เดาแผนก

## การแชร์ LINE

- ไม่มี LINE Bot, LINE API หรือ Token และไม่ส่งอัตโนมัติ
- ใช้เมนูแชร์ของมือถือ (Web Share API) ไฟล์ส่งจากมือถือไปยังแอปที่ HR เลือกโดยตรง ไม่ผ่านเซิร์ฟเวอร์ใด
- ต้องติ๊กยืนยันว่าปลายทางได้รับอนุญาตให้เข้าถึงข้อมูลพนักงานของแผนกนั้นก่อนทุกครั้ง

## ความเสี่ยงที่เหลือ และสิ่งที่ HR ต้องทำ

| ความเสี่ยง | วิธีลดความเสี่ยง |
|---|---|
| เลือกแผนกผิดหน้า | ดูภาพหัวเอกสารทุกครั้งที่เริ่มแผนกใหม่ และแตะ *ดูหน้า* ก่อนแชร์ |
| ส่ง LINE ผิดกลุ่ม | ตรวจชื่อกลุ่มก่อนกดส่งทุกครั้ง |
| ไฟล์ที่บันทึกไว้ใน iCloud Drive จะซิงก์ขึ้น Cloud | ถ้าต้องการเก็บในเครื่อง ให้บันทึกใน *On My iPhone / ในไอโฟนของฉัน* |
| ภาพหน้าจอมีภาพหัวเอกสาร | ห้ามส่งภาพหน้าจอไปที่ใด รวมถึง GitHub Issues |
| มือถือถูกผู้อื่นใช้ | ล้างข้อมูลหลังใช้งาน และตั้งรหัสล็อกหน้าจอ |
| ผู้แก้โค้ดใน repo นี้สามารถเปลี่ยนการทำงานของแอปได้ | จำกัดสิทธิ์เขียน repo เฉพาะผู้ที่ได้รับอนุญาต และตรวจทุกการแก้ไขก่อน merge |
